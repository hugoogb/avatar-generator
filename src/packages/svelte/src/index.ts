// The Svelte component lives in Avatar.svelte, which the consumer's Svelte
// toolchain resolves via the `"svelte"` export condition. This file exists
// so a non-Svelte consumer (e.g. plain TypeScript importing types) still has
// a default entry point.
export {};
