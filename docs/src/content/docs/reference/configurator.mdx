---
title: Configurator
description: Pick a style, tweak every option with form controls, and copy the exact createAvatar() call for the current configuration.
---

import Configurator from "../../../components/Configurator.astro";

Adjust the controls to see how each option changes the generated avatar.
The snippet below the preview updates in real time — copy it straight into
your code.

<Configurator />

The preview uses simplified renderers matching each style's shape. Install
the actual packages for the full output:

- `@avatar-generator/core`
- `@avatar-generator/style-<name>`

See [Installation](/get-started/installation/) for setup and the
[Style Gallery](/reference/gallery/) to compare styles side by side.
